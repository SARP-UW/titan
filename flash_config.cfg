source [find interface/stlink-dap.cfg]
transport select dapdirect_swd

# Single-core (M7-only) — do not create/examine cpu1
set DUAL_CORE 0

source [find target/stm32h7x_dual_bank.cfg]

reset_config srst_only